<div class="contents row">
    <%= form_for @tweet, url: "/tweets/#{@tweet.id}" do|f| %>
      <h3>
        編集する
      </h3>
      <%= f.text_field :text  %>
    <%= f.submit "EDIT" %>
    <% end %>
  </div>
